<!-- 一句话说明这个 PR 做了什么 / One line on what this changes. -->

## What / 改动内容

## Why / 原因

Fixes #

## Checklist / 检查项

- [ ] `swift build -c release` passes / 本地编译通过
- [ ] No new per-frame work on the main thread / 没有给主线程增加每帧开销
- [ ] New UI strings were added to `LKey` with both `zh` and `en` values /
      新增界面文案已加入 `LKey`，中英文都已填写
- [ ] `CHANGELOG.md` updated for user-visible changes / 用户可见的改动已更新 CHANGELOG
- [ ] Deployment target (macOS 13) still holds / 未破坏 macOS 13 部署目标
